import axios from 'axios';

//Using temparary endpoints here for testing. It will be replaced later..
//Move all api urls to a single place.
//Refactor this
export const userForgotPasswordAPI = (user) => {
	return {
		data: {
		  success: true
		}
	}
}